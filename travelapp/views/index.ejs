<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/main.css" />
    <title>Index</title>
  </head>
  <body>
    <div class="container">
      <nav class="navbar navbar-default">
        <a class="btn btn-outline-primary" href="/sessions/new">Log In</a>
        <!-- <a class="btn btn-outline-primary" href="/users/new">Register</a> -->
        <% if(currentUser) { %>
        <form action="/sessions?_method=DELETE" method="POST">
          <input class="btn btn-outline-primary" type="submit" value="Logout" />
        </form>
        <a class="btn btn-outline-primary" href="/travel/new"
          >Create New Post</a
        >
        <% } %>
      </nav>
      <div class="jumbotron">
        <h4>See the World</h4>
        <p>Where will your next journey take you?</p>
      </div>
      <h1>Trending Destinations</h1>

      <% post.forEach((post, id)=> { %>

      <h2><a href="/travel/<%=post.id%>"><%=post.title%></a></h2>
      <a href="/travel/<%=post.id%>"><img src="<%=post.image%>"/></a>

      <br />
      <% }) %>
    </div>
  </body>
</html>
